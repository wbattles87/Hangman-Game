// array of players inside the game, its an array of strings //
const generatedList = ["Deshaun Watson", "Lamar Miller", "Jay Prosch", "Will Fuller", "Deandre Hopkins", "Bruce Ellington", "Stephen Anderson", "Ryan Griffin", "Jeff Allen", "Breno Giacomini", "Kendall Lamm", "Greg Mancz", "Nick Martin", " Jon Weeks", "Jadeveon Clowney", "Christian Covington", "DJ Reader", "JJ Watt", "Benardrick Mckinney", "Whitney Mercilus", "Andre Hal", "Eddie Pleasant", "Johnathan Joseph", "Kareem Jackson", "Kevin Johnson", "Kaimi Fairbairn", "Shane Lechler"];

// let allows you to declare variables that are limited in scope to the block, statement, or expression on which it is used. 
// This is unlike the var keyword, which defines a variable globally, or locally to an entire function regardless of block scope.


// setting variable generatedWord equal to a random generator for an array//
let generatedWord = Math.floor(Math.random() * generatedList.length);
// now setting choosenWord to generatedList which now has the random word generated from above//
let choosenWord = generatedList[generatedWord];
// this is making the new array for the underscores we will have generated by the length of the word we randomly picked
let underScore = [];
let correctGuess = [];
let wrongGuess = [];
console.log(choosenWord);


// this will generate the amount of underscores needed per choosen word
// arrow function used here to keep the scope in check
// easier to see the scope of the function
let generateUnderscores = () => {
	for(i = 0; i < choosenWord.length ; i++)
		{
			// this is essentially adding _ to a blank array we are creating with the amount equal to the word we randomly picked
			underScore.push("_");			
		}
		return underScore;
}

console.log(generateUnderscores()); 


// this will capture the users input, in this case the pressing of a key
document.addEventListener("keypress", (event) => {					
	let keyword = String.fromCharCode(event.keyCode);		// this gets the Unicode value and converts that value into a character
	if(choosenWord.indexOf(keyword) > -1) {					// if the user guesses the right letter, 
		correctGuess.push(keyword);							// adds the correct guess to the correct guess array
		underScore[choosenWord.indexOf(keyword)] = keyword; // this replaces the underscore in the word area with the correct letter guessed
		console.log(correctGuess);

		if(underScore.join(" ") == "choosenWord") {
			alert("You Win");		
		}
	}
	else {
		wrongGuess.push(keyword);
		console.log(wrongGuess);
	}

})